<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{nombre}}</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f4f4f4;
        }
        .product-detail {
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <header>
      <p>Texto de prueba para verificar cambios en la vista</p>
        <h1>Detalles del Producto</h1>
    </header>

    <main>
        <div class="product-detail">
            <table>
                <tr>
                    <th>Nombre</th>
                    <td>{{nombre}}</td>
                </tr>
                <tr>
                    <th>Precio</th>
                    <td>${{precio}}</td>
                </tr>
                <tr>
                    <th>Categoría</th>
                    <td>{{categoria}}</td>
                </tr>
                <tr>
                    <th>Descripción</th>
                    <td>{{descripcion}}</td>
                </tr>
                <tr>
                    <th>Disponible</th>
                    <td>{{#if estado}}Sí{{else}}No{{/if}}</td>
                </tr>
            </table>

            <!-- Formulario para agregar al carrito -->
            <form action="/api/carts/add-product" method="POST">
                <input type="hidden" name="productId" value="{{_id}}">
                <input type="hidden" name="carritoId" value="{{carritoId}}">
                <button type="submit">Agregar al carrito</button>
            </form>
        </div>

        <a href="/products">Volver a la lista de productos</a>
    </main>

    <footer>
        <p>&copy; Agosto 2024 - Carlos Vergara P</p>
    </footer>
</body>
</html>
